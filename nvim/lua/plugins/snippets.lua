-- LazyVim's default config for LuaSnip loads vscode snippets from:
-- ~/.config/nvim/snippets/

-- Just make sure there is a package.json file and a file for your snippets
-- as per the docs

return {
  "L3MON4D3/LuaSnip",
  opts = { cut_selection_keys = "<Tab>" },
}
